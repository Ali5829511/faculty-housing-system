# نظام إدارة المرور الجامعي المتكامل
## Integrated University Traffic Management System

---

## 📋 نظرة عامة

نظام متكامل لإدارة المرور والمركبات في جامعة الإمام محمد بن سعود الإسلامية، يتضمن:

- ✅ **قاعدة بيانات المركبات الشاملة**
- ✅ **قاعدة بيانات المخالفات المرورية**
- ✅ **التعرف التلقائي على اللوحات** (ParkPow API + Plate Recognizer API)
- ✅ **نظام الدفع والاعتراضات**
- ✅ **تقارير وإحصائيات متقدمة**

---

## 🚀 المميزات الرئيسية

### 1. قاعدة بيانات المركبات

**الملف:** `pages/vehicles_database.html`
**النظام:** `js/vehicles-database-system.js`

#### المميزات:
- إضافة، تعديل، حذف المركبات
- بحث متقدم (رقم اللوحة، اسم المالك، رقم الهوية)
- تصفية حسب نوع المالك (طالب، موظف، عضو هيئة تدريس، زائر)
- تصفية حسب الحالة (نشط، معلق، منتهي)
- تسجيل دخول وخروج المركبات
- إدارة الصور والمستندات
- ربط تلقائي مع المخالفات
- تصدير البيانات إلى CSV
- إحصائيات شاملة

#### البيانات المخزنة:
```javascript
{
    id: 'VEH-xxxxx',
    plateNumber: 'رقم اللوحة',
    ownerName: 'اسم المالك',
    ownerType: 'student|staff|faculty|visitor',
    nationalId: 'رقم الهوية',
    mobile: 'رقم الجوال',
    email: 'البريد الإلكتروني',
    make: 'الماركة',
    model: 'الموديل',
    year: 'السنة',
    color: 'اللون',
    vehicleType: 'sedan|suv|truck|van|motorcycle',
    stickerNumber: 'رقم الملصق',
    status: 'active|suspended|expired',
    violationsCount: 0,
    entriesCount: 0,
    // ... المزيد
}
```

---

### 2. قاعدة بيانات المخالفات المرورية

**الملف:** `pages/violations_database.html`
**النظام:** `js/violations-database-system.js`

#### المميزات:
- 12 نوع مخالفة مختلفة
- إدارة الغرامات والدفع
- نظام الاعتراضات
- ربط تلقائي بالمركبات
- بحث متقدم
- تصفية حسب الحالة والخطورة
- تصدير البيانات
- إحصائيات مفصلة

#### أنواع المخالفات:
1. **تجاوز السرعة المحددة** - 500 ريال - 2 نقطة
2. **الوقوف في مكان غير مصرح** - 150 ريال - 1 نقطة
3. **عدم وجود ملصق** - 300 ريال - 2 نقطة
4. **ملصق منتهي الصلاحية** - 200 ريال - 1 نقطة
5. **دخول منطقة محظورة** - 400 ريال - 3 نقاط
6. **قيادة متهورة** - 1000 ريال - 4 نقاط
7. **السير عكس الاتجاه** - 600 ريال - 3 نقاط
8. **إعاقة حركة المرور** - 300 ريال - 2 نقطة
9. **دخول منطقة مشاة** - 250 ريال - 2 نقطة
10. **عدم وجود رخصة قيادة** - 1500 ريال - 6 نقاط
11. **مركبة غير مصرح لها** - 500 ريال - 3 نقاط
12. **مخالفة أخرى** - 200 ريال - 1 نقطة

#### البيانات المخزنة:
```javascript
{
    id: 'VIO-xxxxx',
    violationNumber: 'V-2025-000001',
    plateNumber: 'رقم اللوحة',
    vehicleId: 'معرف المركبة',
    violationType: 'نوع المخالفة',
    fine: 500,
    points: 2,
    severity: 'low|medium|high|critical',
    location: 'الموقع',
    status: 'pending|paid|appealed|cancelled',
    isPaid: false,
    isAppealed: false,
    // ... المزيد
}
```

---

### 3. التكامل مع ParkPow API

**الملف:** `js/parkpow-advanced-integration.js`

#### المميزات:
- التعرف التلقائي على اللوحات
- الكشف التلقائي عن المخالفات
- تسجيل الدخول التلقائي
- التنبيهات الأمنية (مركبات غير مسجلة، مركبات معلقة)
- المزامنة التلقائية مع ParkPow
- معالجة الصور

#### API Tokens:
```javascript
// Plate Recognizer API
apiToken: '560a4728fc1f0fee1f76d1eb67f001d762a941d9'

// ParkPow API
apiToken: '64fbe3cdf0861b97a5e08bc9d5116a3d6d17ab66'
licenseKey: '6mBNSb5L6W'
```

#### الاستخدام:
```javascript
// معالجة صورة لوحة
const result = await parkpowAdvanced.processPlateImage(imageFile);

// تسجيل دخول تلقائي
const entry = await parkpowAdvanced.autoRecordEntry(plateData, gateInfo);

// كشف مخالفة تلقائي
const violation = await parkpowAdvanced.autoDetectViolation(
    plateData, 
    'no_sticker', 
    gateInfo
);

// مزامنة البيانات
const sync = await parkpowAdvanced.syncWithParkPow();
```

---

## 📁 هيكل الملفات

```
N-M/
├── pages/
│   ├── vehicles_database.html          # صفحة قاعدة بيانات المركبات
│   ├── violations_database.html        # صفحة قاعدة بيانات المخالفات
│   ├── databases_hub.html              # مركز قواعد البيانات
│   └── home.html                       # الصفحة الرئيسية
│
├── js/
│   ├── vehicles-database-system.js     # نظام قاعدة بيانات المركبات
│   ├── violations-database-system.js   # نظام قاعدة بيانات المخالفات
│   ├── parkpow-advanced-integration.js # التكامل مع ParkPow
│   └── parkpow-integration.js          # التكامل الأساسي
│
├── css/
│   └── imamu-brand-identity.css        # الهوية البصرية للجامعة
│
└── config/
    └── parkpow_config.json             # إعدادات ParkPow
```

---

## 🎨 الهوية البصرية

### الألوان:
- **اللون الأساسي:** `#045D84` (أزرق داكن)
- **اللون الثانوي:** `#B7A362` (ذهبي)
- **الأبيض:** `#FFFFFF`

### الخطوط:
- **Bahij TheSansArabic** (الخط الأساسي)
- **AL-Mohanad** (الخط الثانوي)

---

## 💾 التخزين

جميع البيانات يتم تخزينها في **LocalStorage**:

```javascript
// المركبات
localStorage.getItem('vehicles_database')

// المخالفات
localStorage.getItem('violations_database')

// التنبيهات الأمنية
localStorage.getItem('security_alerts')

// بيانات ParkPow المحلية
localStorage.getItem('parkpow_vehicles')
localStorage.getItem('parkpow_sync_time')
```

---

## 🔧 الوظائف الرئيسية

### قاعدة بيانات المركبات:

```javascript
// إضافة مركبة
vehiclesDB.addVehicle(vehicleData)

// تحديث مركبة
vehiclesDB.updateVehicle(vehicleId, updates)

// حذف مركبة
vehiclesDB.deleteVehicle(vehicleId)

// البحث
vehiclesDB.search(criteria)

// تسجيل دخول
vehiclesDB.recordEntry(vehicleId, entryData)

// إحصائيات
vehiclesDB.getStatistics()

// تصدير
vehiclesDB.exportToCSV()
```

### قاعدة بيانات المخالفات:

```javascript
// إضافة مخالفة
violationsDB.addViolation(violationData)

// دفع مخالفة
violationsDB.payViolation(violationId, paymentData)

// الاعتراض على مخالفة
violationsDB.appealViolation(violationId, appealData)

// البحث
violationsDB.search(criteria)

// إحصائيات
violationsDB.getStatistics()

// تصدير
violationsDB.exportToCSV()
```

---

## 📊 الإحصائيات

### إحصائيات المركبات:
- إجمالي المركبات
- المركبات النشطة
- المركبات المعلقة
- المركبات المنتهية
- التوزيع حسب نوع المالك
- إجمالي المخالفات
- إجمالي الدخول

### إحصائيات المخالفات:
- إجمالي المخالفات
- المخالفات المعلقة
- المخالفات المدفوعة
- المخالفات المعترض عليها
- إجمالي الغرامات
- الغرامات المدفوعة
- الغرامات المعلقة
- التوزيع حسب الخطورة
- أكثر المخالفات شيوعاً
- نسبة التحصيل

---

## 🌐 الروابط

### الموقع المنشور:
https://hilarious-narwhal-1a5720.netlify.app/

### المستودع على GitHub:
https://github.com/Ali5829511/N-M

### الصفحات الرئيسية:
- **الصفحة الرئيسية:** `/pages/home.html`
- **مركز قواعد البيانات:** `/pages/databases_hub.html`
- **قاعدة بيانات المركبات:** `/pages/vehicles_database.html`
- **قاعدة بيانات المخالفات:** `/pages/violations_database.html`

---

## 📱 التصميم المتجاوب

النظام متجاوب بالكامل ويعمل على:
- 💻 Desktop (1920px+)
- 💻 Laptop (1366px - 1920px)
- 📱 Tablet (768px - 1366px)
- 📱 Mobile (< 768px)

---

## 🔐 الأمان

- جميع البيانات محلية في LocalStorage
- لا يتم إرسال بيانات حساسة إلى الخوادم
- API Tokens مشفرة
- التحقق من الصلاحيات قبل العمليات الحساسة

---

## 🚀 التطوير المستقبلي

### المخطط:
- [ ] تكامل مع قاعدة بيانات سحابية (Neon PostgreSQL)
- [ ] نظام المستخدمين والصلاحيات
- [ ] تطبيق موبايل (Android/iOS)
- [ ] تقارير PDF متقدمة
- [ ] لوحة تحكم تحليلية
- [ ] نظام الإشعارات
- [ ] تكامل مع بوابة الدفع الإلكتروني
- [ ] API للتكامل مع أنظمة خارجية

---

## 👨‍💻 المطور

**جامعة الإمام محمد بن سعود الإسلامية**
قسم تقنية المعلومات

---

## 📄 الترخيص

هذا النظام مملوك لجامعة الإمام محمد بن سعود الإسلامية
جميع الحقوق محفوظة © 2025

---

## 📞 الدعم الفني

للدعم الفني والاستفسارات:
- البريد الإلكتروني: support@imamu.edu.sa
- الهاتف: +966 11 xxxxxxx

---

**تم التطوير بواسطة:** Manus AI Agent
**التاريخ:** نوفمبر 2025
**الإصدار:** 2.0.0
